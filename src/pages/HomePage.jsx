import React from 'react';
import CategoryList from '../components/CategoryList.jsx';

// Extrae la función con la información que se visualizará en la página de entrada.
const HomePage = () => (
  <div>
    <CategoryList />
  </div>
);

export default HomePage;
